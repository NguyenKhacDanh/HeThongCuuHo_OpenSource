<!DOCTYPE html>
<html lang="vi" data-bs-theme="light">
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"] - RescueHub</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/site.css" />
</head>
<body>
    <header class="navbar navbar-expand-lg border-bottom mb-3">
        <div class="container-fluid">
            <a class="navbar-brand fw-bold" asp-controller="Public" asp-action="Index">RescueHub</a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="mainNav">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Public" asp-action="RequestHelp">Yêu cầu cứu hộ</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Public" asp-action="SafeReport">Tôi an toàn</a>
                    </li>

                    @if (User.IsInRole("Rescuer"))
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Rescuer" asp-action="Dashboard">Rescuer Dashboard</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Rescuer" asp-action="Map">Bản đồ cứu hộ</a>
                        </li>
                    }
                    @if (User.IsInRole("Admin"))
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Admin" asp-action="Dashboard">Admin Dashboard</a>
                        </li>
                    }
                </ul>

                <div class="d-flex align-items-center gap-2">
                    <button id="themeToggle" class="btn btn-outline-secondary btn-sm" type="button">
                        🌓
                    </button>

                    @if (User.Identity?.IsAuthenticated ?? false)
                    {
                        <span class="text-muted small me-2">@User.Identity!.Name</span>
                        <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                            <button class="btn btn-outline-danger btn-sm" type="submit">Đăng xuất</button>
                        </form>
                    }
                    else
                    {
                        <a class="btn btn-outline-primary btn-sm" asp-controller="Account" asp-action="Login">Đăng nhập</a>
                        <a class="btn btn-primary btn-sm" asp-controller="Account" asp-action="RegisterRescuer">Đăng ký Rescuer</a>
                    }
                </div>
            </div>
        </div>
    </header>

    <div class="container mb-5">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top py-3 mt-auto text-center small">
        <div class="container">
            RescueHub &copy; @DateTime.Now.Year - Hệ thống hỗ trợ cứu hộ mùa lũ
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
